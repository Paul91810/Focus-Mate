import 'package:flutter/material.dart';

class AiQuoteGeneratorScreen extends StatelessWidget {
  const AiQuoteGeneratorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}